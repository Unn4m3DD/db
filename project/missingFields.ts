interface cpm_t {
    station_id: number, //missing field
}

interface cam_t {
    station_id: number, //missing field
    last_power_status: number, //missing field
    current_app_version: number//missing field
}

interface vam_t {
    station_id: number, //missing field
    last_power_status: number, //missing field
    configured_language: string, //missing field
    current_app_version: number//missing field
}

interface denm_t {
    station_id: number, //missing field
    configured_language: string, //missing field
    current_app_version: number//missing field
}
